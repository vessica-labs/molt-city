{
  "name": "@molt-city/client",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@molt-city/shared": "workspace:*"
  },
  "scripts": {
    "build": "pnpm --filter @molt-city/shared build && tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "test": "vitest run --passWithNoTests"
  }
}
